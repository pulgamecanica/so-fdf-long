#include "so_long/so_long_menu.h"

void so_long_menu_on_enter(t_app *app)
{
  (void)app;
  // TODO: initialize so_long menu (load buttons, etc.)
}

void so_long_menu_on_exit(t_app *app)
{
  (void)app;
  // TODO: cleanup so_long menu resources
}

void so_long_menu_update(t_app *app)
{
  (void)app;
  // TODO: handle input & animations for so_long menu
}

void so_long_menu_render(t_app *app)
{
  (void)app;
  // TODO: draw so_long menu (buttons, labels, etc.)
}
